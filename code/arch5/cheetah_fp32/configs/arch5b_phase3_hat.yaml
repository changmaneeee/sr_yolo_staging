# =============================================================================
# Arch5B Phase 3 HAT — Joint Full Training Config
# 변경: YOLO weight → HAT-trained YOLO (detector matching rule 적용)
# =============================================================================

data:
  hr_root: "/home/jovyan/changmin/dark_vessel_research/smart_airbus_data"
  hr_val_images: "images/val"
  hr_val_labels: "labels/val"
  lr_root: "/home/jovyan/changmin/dark_vessel_research/smart_airbus_data_lr"
  lr_val_images: "images/val"
  lr_val_labels: "labels/val"
  upscale_factor: 4

model:
  pipeline: arch5b_fusion
  sr_type: hat
  use_checkpoint: true  # Phase3 full-unfreeze backward OOM 방지 (gradient checkpointing)
  detector_input: "sr"

  sr:
    type: hat
    weights: "/home/jovyan/changmin/dark_vessel_research/handoff_cheetah_20260525/code/weights/sr_finetuned/hat/best.pt"
    hat:
      variant: base
      pretrain_path: "/home/jovyan/changmin/dark_vessel_research/handoff_cheetah_20260525/code/weights/sr_finetuned/hat/best.pt"

  yolo:
    weights_path: "/home/jovyan/changmin/dark_vessel_research/handoff_cheetah_20260525/code/weights/yolo_8s_hat/weights/best.pt"
    num_classes: 1
    imgsz: 640

  fusion:
    use_cross_attention: true
    use_cbam: true
    num_heads: 4
    init_mode: "identity"

training:
  sr_weight: 0.3
  det_weight: 1.0
  phase: 3
  freeze: "none"

evaluation:
  conf_threshold: 0.25
  iou_threshold: 0.50
  max_det: 300

logging:
  save_dir: "iac_lab/runs/arch5b_phase3_hat"
